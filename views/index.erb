<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <a href="<%= url("contacts/#{contact.id}") %>">
         <%= contact.name %></a>
         - <%= contact.phone_number %></li>
    </li>
  <% end %>
</ul>
